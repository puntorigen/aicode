{
  "Usage: $0 <command> [options]": "Usage: $0 <command> [options]",
  "Process the input": "Process the input",
  "Run with debug output": "Run with debug output",
  "Language for the output": "Language for the output",
  "Input string to process": "Input string to process",
  "parsing": "parsing",
  "Confirm each AI action before executing it": "Confirm each AI action before executing it",
  "Force using the local model provider (Ollama/LM Studio) for this run": "Force using the local model provider (Ollama/LM Studio) for this run",
  "Force agent mode: a multi-step tool-using loop instead of a single action": "Force agent mode: a multi-step tool-using loop instead of a single action",
  "Only print the final answer (no header, steps or footer)": "Only print the final answer (no header, steps or footer)",
  "Open the interactive TUI session": "Open the interactive TUI session",
  "Install an Agent Skill from skills.sh (e.g. owner/repo), then exit": "Install an Agent Skill from skills.sh (e.g. owner/repo), then exit",
  "With --install-skill: install the skill globally (~/) instead of into the project": "With --install-skill: install the skill globally (~/) instead of into the project"
}
